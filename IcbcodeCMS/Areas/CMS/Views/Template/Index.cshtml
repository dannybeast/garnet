@{
    ViewBag.Title = "Шаблоны данных";
    Layout = "~/Areas/CMS/Views/Shared/_LayoutMain.cshtml";
}

<div id="nav_panel">
    <a href="@Url.Action("Edit", "Template")"><button class="button_element">Добавить шаблон данных</button></a>
</div>

@if (ViewBag.items.Count == 0)
{
<p><b>Данных нет</b></p>
}
else
{
<table class="content_table" width="100%"  cellpadding="0" cellspacing="0">
    <tr class="ui-widget-header">
        <th class="first">Наименование</th>
        <th class="first">Объявление</th>
        <th>&nbsp;</th>
        <th>&nbsp;</th>
    </tr>
    <tbody class="sortable" data-update-url="@Url.Action("UpdateOrder", "Template")">
    @foreach (var item in ViewBag.items)
    {
    <tr>
        <td class="first">
            @item.template_friendly_name
            <input type="hidden" name="id" value="@item.template_id" />
        </td>
        <td class="first">@item.template_name</td>
        <td><a href="@Url.Action("Edit", "Template", new { template_id = item.template_id })">Редактировать</a></td>
        <td>
            <a onclick="return confirm('Вы уверены что хотите удалить шаблон?');" href="@Url.Action("Remove", "Template", new { template_id = item.template_id })">Удалить</a>
        </td>
    </tr>
    }
    </tbody>
</table>
}